{
  "name": "tries",
  "version": "1.0.0",
  "description": "Tries data structure.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "npm run compile && ts-node node_modules/jasmine/bin/jasmine --config=test/support/jasmine.json",
    "compile": "tsc src/*.ts  --outDir dist --pretty"
  },
  "author": "Ahmet ATAY",
  "license": "ISC",
  "devDependencies": {
    "@types/jasmine": "^3.5.10",
    "jasmine": "^3.5.0",
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3"
  }
}
